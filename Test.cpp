#include <Windows.h>


int APIENTRY wWinMain(_In_ HINSTANCE hInstance,
    _In_opt_ HINSTANCE hPrevInstance,
    _In_ LPWSTR    lpCmdLine,
    _In_ int       nCmdShow) 
{
    /*
    WinBox* mainWin = new WinBox(hInstance);
    if (mainWin) {
        mainWin->Create(L"TestDemo", L"MainWindow", 0, 0, 640, 480);

        WinBox* child1 = new WinBox(mainWin->GetHandle());
        if (child1) {
            child1->Create(L"click", L"Button", 0, 0, 250, 50);
        }

        mainWin->MessageControl();
    }
    */

    return 0;
}